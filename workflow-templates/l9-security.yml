# Quantum-L9 org starter — Security scan pipeline (gitleaks, Bandit, Semgrep, pip-audit). Calls l9-ci-core kernel; do not add logic here.
# LEGACY (frozen @v1) — historical kernel starter. New work starts from l9-ci-pack/README.md (v2).
name: "L9 Security Scan"
on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: '0 6 * * 1'

jobs:
  l9_security:
    uses: Quantum-L9/l9-ci-core/.github/workflows/security.yml@2b330a5aab90cd7781bef08f14c5e7904b61bc56
    secrets: inherit
    with:
      python-version: "3.12"
      run-npm-audit: false
