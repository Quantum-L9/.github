# Quantum-L9 org starter — Versioned release build and PyPI publish. Calls l9-ci-core kernel; do not add logic here.
# LEGACY (frozen @v1) — historical kernel starter. New work starts from l9-ci-pack/README.md (v2).
name: "L9 Release"
on:
  push:
    tags: ['v*.*.*']

jobs:
  l9_release:
    uses: Quantum-L9/l9-ci-core/.github/workflows/release-publish.yml@2b330a5aab90cd7781bef08f14c5e7904b61bc56
    secrets: inherit
    with:
      python-version: "3.12"
      publish-to-pypi: true
